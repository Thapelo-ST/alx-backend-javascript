export default function groceriesList() {
    const mapper = new Map();

    mapper.set('Apples', 10);
    mapper.set('Tomatoes', 10);
    mapper.set('Pasta', 1);
    mapper.set('Rice', 1);
    mapper.set('Banana', 5);

    return mapper;
}